#include <iostream>

int poly(int f, int r) //f = number of squares in the first row, r = remaining squares that can be used
{
	if(r == 1)
}
int main()
{
	int C = 0, n = 0;
	cin >> C;
	for(int i = 0; i < C; i++){
		cin >> n;
	}
}